# 全局监控项目 - 模块会话表

| 模块 | 会话 ID | 状态 | 说明 |
| --- | --- | --- | --- |
| 检测模块 | 01a00d70-90f9-7480-be9e-a3bcade074cb | 运行中 | 只负责检测，不执行、不汇报 |
| 检测模块（旧） | 01a00b28-415b-7381-8e87-0294a62733d7 | 已结束/systemError | 旧检测会话，已由新检测会话替代 |
| 执行模块 | 01a00b1c-48e0-7461-8b15-226be7d6342b | 运行中 | 接收检测结果并执行修复 |
| 汇报模块 | 01a00b1c-4fcf-7b40-bc43-9058ff543214 | 运行中 | 接收执行结果并向用户汇报 |

## 协作流程

1. 检测模块发现问题时，用 send_message_to_thread 移交给执行模块。
2. 执行模块完成修复后，把结果发送给汇报模块。
3. 汇报模块向用户汇报，并与用户交互。

## 当前任务

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| 全局监控范围扩展 + 工作流化 | 已完成 | 工作流文档已固化，AGENTS 与工作流技能已沉淀，自动化 prompt 已扩展，量化汇报已启用 |
| 监控频率调整（15分钟 + 重启后检测） | 已完成 | 心跳改为每 15 分钟，重启后第一次心跳强制完整检测 |
| 自我修复与优化 | 已完成 | 心跳必须留痕；空转自动重试并升级；元监控；修复后评估规则沉淀 |
| 检测模块修复 + 持续监控其他会话 | 已完成 | 429 限流重试、空转强制留痕、每轮跟踪运行中的非监控会话 |
| 指令目标化改造 | 已完成 | 所有模块指令改为先创建目标、完成后再结束 |
| 全局目标化规则强化 | 已完成 | 每次会话必须先创建目标、以目标形式完成任务，未完成不得结束 |

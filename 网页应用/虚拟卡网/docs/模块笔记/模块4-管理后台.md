# 模块4 管理后台实现笔记

## 1. 职责范围

本模块只交付管理后台：`src/app/admin/**`（鉴权布局与后台页面）、`src/components/admin/**`（后台共享组件）、`docs/模块笔记/模块4-管理后台.md`。未修改 `src/lib/**`、`src/app/api/**`、前台/个人中心页面、`package.json`、`API.md`、`README.md`。

## 2. 交付内容

### 2.1 后台布局

- `src/app/admin/layout.tsx` 为服务端布局：读取会话，未登录或非 admin 跳转 `/auth/login?next=/admin`。
- `src/components/admin/nav.tsx` 提供桌面左侧导航与移动端顶部导航（概览、商品、分类、卡密、发卡记录、订单），并包含返回商城、退出登录入口；侧栏定位在全局 sticky 头部（h-16）下方，避免遮挡模块2 的全局头部。

### 2.2 页面

- `/admin` 概览：今日/累计销售额与订单数、用户数（接口未提供，显示 `—`）、在售商品数（由 `/api/admin/products?status=active` 计算）、低库存预警、最近订单、分类销售排行，数据主要来自 `GET /api/admin/stats`。
- `/admin/products` 商品管理：搜索/分类/状态筛选，封面、名称、分类、价格、库存、销量、状态表格；新增/编辑弹窗（名称、分类、价格、原价、描述、封面、预警阈值、上下架）；行内上架/下架与删除；后端 409 删除保护直接提示后端文案。
- `/admin/categories` 分类管理：列表、新增/编辑（名称、别名、排序）、删除确认；删除后商品自动变为未分类（后端处理）。
- `/admin/cards` 卡密管理：商品选择与可用/总数库存卡；批量导入 textarea（每行一条，展示 `导入 N 条，跳过 M 条`）；状态筛选与内容搜索、分页、单条删除；已售卡密显示锁定且不可删。
- `/admin/claims` 发卡记录：调用 `GET /api/admin/integration/claims`，展示时间、流水号、外部订单号、商品、数量、卡密 ID，支持商品筛选、外部订单号搜索、分页与 JSON 查看。
- `/admin/orders` 订单管理：状态筛选、订单号/用户名搜索、分页；行内展开商品明细；已发货订单展开时通过 `/api/admin/cards?status=sold&product_id=...` 拉取并匹配 `order_item_id` 展示已发卡密；待支付可取消、已支付可发货。

### 2.3 共享层

- `src/components/admin/api.ts`：JSON 请求封装与后端错误文案提取。
- `src/components/admin/ui.tsx`：按钮、图标按钮、弹窗、确认框、徽章、分页、表单控件、空态等。
- `src/components/admin/status.tsx` / `format.ts`：订单/卡密/商品状态徽章，金额与 Asia/Shanghai 时间格式化。

## 3. 验证结果

环境：捆绑 Node 24 + pnpm；`pnpm typecheck` 通过（0 错误）；`NEXT_DIST_DIR=.next-m4` + `next dev -p 3103` 启动成功，验证后已停止服务器，未运行 `pnpm build`，未重置数据库。

使用 `admin/admin123` 验证：

- 未登录访问 `/admin` 返回 307 跳转 `/auth/login?next=/admin`；登录后 `/admin` 及 4 个功能页全部 200。
- 统计接口正常；低库存预警能识别种子数据中的低库存商品。
- 分类新增/编辑/删除成功（删除后测试商品回到未分类）。
- 商品新增、PATCH 改价、上下架切换成功；删除已有售出卡密的商品返回 409，文案为「商品已有售出卡密，无法删除，请改为下架」。
- 卡密批量导入 2 条成功；重复导入返回 `imported=0, skipped=2`；内容搜索、状态筛选、单条删除成功；已售卡密删除返回 409。
- 订单：创建待支付订单后管理端取消成功；状态/用户名筛选正确。因当前接口无“生成 paid 订单”的路径，为验证发货流程，将测试订单状态直接改为 `paid` 后调用 `PATCH /api/admin/orders/[id]` 发货成功，返回订单 `delivered` 且绑定 1 张卡密；再次发货返回 409。
- 验证产生的测试分类、商品、卡密与订单已全部清理，数据库恢复为验证前状态（4 个种子分类、0 订单）。

## 4. 遗留问题

- `GET /api/admin/stats` 未返回用户数字段，概览页「用户数」显示 `—`；若模块1 后续在 stats 中补充 `user_count`，可去掉占位逻辑。在售商品数目前由 `/api/admin/products?status=active` 计算。
- `/api/admin/orders` 列表不含已发卡密，订单展开页按商品逐个请求 `/api/admin/cards?status=sold` 并用 `order_item_id` 匹配；单商品超过 200 张卡密时该商品明细可能被截断（当前真实订单量级不受影响）。
- 当前支付流程把 `pending` 直接推到 `delivered`，管理端「发货」只对 `paid` 订单可用；若后续引入人工收款流程，可复用模块1 的 `deliverOrder`。
- dev 验证期间出现过一次 `/api/admin/cards` 的瞬时 500（Next 开发态 manifest 重编译竞态），随后重试返回 200，未再复现，不影响功能。

## 5. 续作：外部发卡记录页面

### 5.1 交付内容

- 新增 `src/app/admin/claims/page.tsx`：调用 `GET /api/admin/integration/claims`，表格展示时间、`claim_no`、外部订单号、商品、数量、卡密 ID，操作列支持「查看 JSON」弹窗（展示完整记录）。
- 支持商品筛选（`product_id`）与外部订单号模糊搜索（`external_order_no`）、分页（每页 50 条）。
- `card_ids` 按 JSON 数组字符串解析后展示为卡密 ID 列表；解析失败时回退为逗号/空格分隔文本。
- `src/components/admin/nav.tsx` 新增「发卡记录」导航入口（桌面侧栏与移动端顶部导航同步）。
- 复用 `src/components/admin/` 现有组件，中文界面、响应式、表格行高稳定。

### 5.2 验证结果

- `pnpm typecheck` 通过，0 错误。
- 启动 dev（`.next-m4` + 端口 3103，配置 `INTEGRATION_API_TOKEN`）后，使用 `admin/admin123` 登录验证：
  - `GET /admin/claims` 返回 200，页面包含「发卡记录」标题。
  - `GET /api/admin/integration/claims` 初始返回 `total: 0`。
  - 通过 `POST /api/integration/cards/claim` 创建 1 条真实流水（Steam 50 元 ×1），随后按外部订单号查询返回 1 条记录，`card_ids` 为 `[1]`。
  - 页面表格字段与 API 契约一致。
- 验证完成后已删除测试流水、恢复卡密为可用并回滚销量，数据库恢复为验证前状态（`claims total=0`、商品 1 库存 60/销量 0）。
- dev 服务器已停止；未运行 `pnpm build`。

### 5.3 遗留问题

- 页面当前只实现任务要求的商品筛选与外部订单号搜索；API 还支持 `created_from`/`created_to` 时间范围筛选，暂未接入界面。
